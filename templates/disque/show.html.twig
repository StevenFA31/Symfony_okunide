{% extends 'base.html.twig' %}

{% block title %}Disque{% endblock %}

{% block body %}
    <h1>Disque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disque.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ disque.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ disque.Date ? disque.Date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_disque_index') }}">back to list</a>

    <a href="{{ path('app_disque_edit', {'id': disque.id}) }}">edit</a>

    {{ include('disque/_delete_form.html.twig') }}
{% endblock %}
